diff --git a/dist/index.d.ts b/dist/index.d.ts
index 07f25246dd39c88327d74b57733fcef542d65b57..6fa53e8e857a87072c75cf242189d0a5bb5d8e53 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,17 +1,17 @@
-import { MarkdownItTexOptions } from "@mdit/plugin-tex";
-import { KatexOptions, Token } from "katex";
-import MarkdownIt from "markdown-it";
+import type { MarkdownItTexOptions } from "@mdit/plugin-tex";
+import type { KatexOptions, Token } from "katex";
+import type { MarkdownExit, MarkdownExitEnv } from "markdown-exit";
 
 //#region src/options.d.ts
-type KatexLogger<MarkdownItEnv = unknown> = (errorCode: "unknownSymbol" | "unicodeTextInMathMode" | "mathVsTextUnits" | "commentAtEnd" | "htmlExtension" | "newLineInDisplayMode", errorMsg: string, token: Token, env: MarkdownItEnv) => "error" | "warn" | "ignore" | boolean | undefined;
+type KatexLogger<MarkdownExitEnv = {}> = (errorCode: "unknownSymbol" | "unicodeTextInMathMode" | "mathVsTextUnits" | "commentAtEnd" | "htmlExtension" | "newLineInDisplayMode", errorMsg: string, token: Token, env: MarkdownExitEnv) => "error" | "warn" | "ignore" | boolean | undefined;
 type TeXTransformer = (content: string, displayMode: boolean) => string;
-interface MarkdownItKatexOptions<MarkdownItEnv = unknown> extends KatexOptions, Pick<MarkdownItTexOptions, "allowInlineWithSpace" | "delimiters" | "mathFence"> {
+interface MarkdownItKatexOptions<MarkdownExitEnv = {}> extends KatexOptions, Pick<MarkdownItTexOptions, "allowInlineWithSpace" | "delimiters" | "mathFence"> {
   /**
    * Error logger
    *
    * 错误日志记录器
    */
-  logger?: KatexLogger<MarkdownItEnv>;
+  logger?: KatexLogger<MarkdownExitEnv>;
   /**
    * transformer on output content
    *
@@ -21,14 +21,14 @@ interface MarkdownItKatexOptions<MarkdownItEnv = unknown> extends KatexOptions,
 }
 //#endregion
 //#region src/plugin.d.ts
-declare const katex: <MarkdownItEnv = unknown>(md: MarkdownIt, {
+declare const katex: <MarkdownExitEnv = {}>(md: MarkdownExit, {
   allowInlineWithSpace,
   delimiters,
   mathFence,
   logger,
   transformer,
   ...userOptions
-}?: MarkdownItKatexOptions<MarkdownItEnv>) => void;
+}?: MarkdownItKatexOptions<MarkdownExitEnv>) => void;
 //#endregion
 export { KatexLogger, MarkdownItKatexOptions, TeXTransformer, katex };
 //# sourceMappingURL=index.d.ts.map
