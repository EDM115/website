name: Ensures the PR doesn't break the build

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - unlocked
  workflow_dispatch:

jobs:
  pr-check:
    name: "PR Check"
    runs-on: ubuntu-slim
    timeout-minutes: 10

    steps:
      - name: "Checkout"
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: "Install pnpm"
        uses: pnpm/action-setup@v4
      
      - name: "Bump Node.js to v24"
        uses: actions/setup-node@v6
        with:
          check-latest: true
          node-version: lts/krypton

      - name: "Install dependencies"
        run: pnpm i --frozen-lockfile && pnpm install-docfind:linux && pnpm prebuild

      - name: "Types check"
        run: pnpm typecheck

      - name: "Lint check"
        run: pnpm lint

      - name: "Build check"
        run: pnpm generate  # Builds Client and Server SSR + Generates SSG

      - name: "Upload artifact"
        uses: actions/upload-artifact@v6
        with:
          name: nuxt-output
          path: ./.output/public
